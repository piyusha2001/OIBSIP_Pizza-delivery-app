import { Button } from '@chakra-ui/react';
import React from 'react';

export default function Checkout({ subtotal }) {
	return (
		<Button backgroundColor=' #b33030' color='white' width='100%'>
			PAY NOW
		</Button>
	);
}
